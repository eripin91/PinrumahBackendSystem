using Microsoft.AspNetCore.Authentication;
using Microsoft.AspNetCore.Authentication.AzureADB2C.UI;
using Microsoft.AspNetCore.Authentication.Cookies;
using Microsoft.AspNetCore.Mvc;
using System;
using System.Collections.Generic;
using System.Linq;
using System.Threading.Tasks;

namespace PinBackendSystem.Users.Controllers
{
    public class AccountController : Controller
    {
        //public IActionResult CheckSignIn()
        //{
        //    //add code here to check if AzureAD identity exists in user table in local database
        //    //if not then insert new user record into local user table

        //    return Redirect("ss");
        //}
        ////
        //// GET: /Account/SignIn
        //[HttpGet]
        //public IActionResult SignIn()
        //{
        //    return Redirect("ss");
        //}

        //[HttpGet]
        //public IActionResult SignOut()
        //{
        //    //redirect to where user after logout
        //    //return RedirectToAction("ss/" + nameof(HomeController.Index), "Home");
        //    return Redirect("ss");
        //}
    }
}
